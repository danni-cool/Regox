import type { IslandMap } from './types.ts'

// Pass 1 stub — M3 replaces this with the real DFS Island detector.
export function detectIslands(_source: string, _filePath: string): IslandMap {
  return new Map()
}
