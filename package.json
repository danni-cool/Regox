{
  "name": "regox",
  "private": true,
  "scripts": {
    "build:packages": "pnpm -r --filter './packages/*' build",
    "dev": "pnpm --filter mvp dev"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
